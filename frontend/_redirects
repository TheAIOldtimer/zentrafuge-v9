# Serve index.html at root (login page)
/    /index.html    200

# Allow clean URLs for html folder pages
/html/*  /html/:splat  200

# Catch-all: redirect everything else to index.html (for client-side routing)
/*    /index.html   200
